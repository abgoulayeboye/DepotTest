
pipeline {
    agent any

    stages {
        stage('Compiler') {
            steps {
                mvn compile
            }
        }
         stage('Test') {
            steps {
                mvn test
            }
        }
         stage('Deploy') {
            steps {
                echo 'Deploy App'
            }
        }
    }
}
